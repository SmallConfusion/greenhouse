services:
  controller:
    image: ghcr.io/smallconfusion/greenhouse
    environment:
      RUST_LOG: "debug"
    volumes:
      - ./config.yaml:/app/config.yaml
    logging:
      options:
        max-size: "10m"
        max-file: "3"

    restart: no
    devices: []
